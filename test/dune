(test
 (name test)
 (libraries
  merlint
  merlint_doc
  ocaml-merlin
  alcotest
  compiler-libs.common
  eio
  eio_main
  fpath
  merlin-lib.ocaml_parsing
  merlin-lib.ocaml_typing
  unix
  logs.fmt
  fmt
  fmt.tty))

; TODO: Re-enable merlint self-check after fixing all issues
; (rule
;  (alias runtest)
;  (deps
;   %{bin:merlint}
;   (source_tree ../lib)
;   (source_tree ../bin))
;  (action
;   (run merlint ../lib ../bin)))

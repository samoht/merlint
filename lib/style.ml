(** Legacy style module - all checks have been moved to rules/*.ml *)

(** Legacy function for unit tests - returns empty list since all checks moved
    to rules/*.ml *)
let check (_typedtree : Typedtree.t) = []

(library
 (name version)
 (libraries dune-build-info))

(rule
 (target git_hash.ml)
 (deps
  (env_var PROJECT_VERSION))
 (action
  (with-stdout-to
   %{target}
   (bash "printf 'let hash = \"%s\"\\n' \"${PROJECT_VERSION:-dev}\""))))
